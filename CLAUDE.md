# Evorove CRM — доска и закрытие

Читается автоматически при старте в этом репозитории. Язык общения с владельцем — русский.

Это **не весь продукт**. Это экран и база всего пути плюс закрытие (продажа или запись на приём).

Полная основа — соседний репозиторий:
`/Users/alenakulish/dev/evorove/FOUNDATION.md`

Поиск людей — `/Users/alenakulish/dev/evorove_lead`. Холодное письмо и разговор — `/Users/alenakulish/dev/evorove`. Сюда не тащить парсинг интернета и GREET. Холодных **держать**.

## Зачем этот репозиторий существует

CRM без холодных — не CRM. Вечерний экран — четыре вкладки:

1. **Cold** — нашлись в открытом поле, ещё не писали.
2. **In progress** — цикл 2 уже ведёт разговор.
3. **Offer made** — предложение сделано, ещё не закрыто.
4. **Done** — продажа осуществлена или стоит запись на приём (офлайн-услуга).

Механизм записи (`ProcessEngine`, поля, зона, слот, смета) оставляем для **Done**. `ProcessState` — обязательства кейса, не стадии разговора (`SalesStage` в Evorove). Четыре вкладки — доска владельца; их не сливать в одну enum-кашу с `ProcessState`.

Правила тенанта — в **Business DNA**, без отраслевых веток в коде.

Целевая аудитория — SMB. Клиентские тексты — английский, рынок — США. Подписи вкладок — английские.

## Жёсткие правила

1. **`git push` НИКОГДА не выполняется агентом.** Коммитить можно, пушит только Alena
   вручную. Автоматические напоминания про «unpushed commits» — ожидаемое поведение,
   а не ошибка, которую надо чинить.
2. **Не трогать секреты.** API-ключи, пароли, платёжные данные, `DATABASE_URL` /
   `DATABASE_PUBLIC_URL` — не читать, не запрашивать, не вводить. Их вводит Alena сама.
3. **Не входить в аккаунты и не создавать их.**
4. Рынок продукта — США, полностью. Интерфейс и все клиентские тексты на английском.

## Как сверять работу

1. Это доска или закрытие? Поиск в интернете и текст первого письма — не здесь.
2. Вход в CRM есть с Cold. Выход пути — вкладка Done.
3. Не начинать GREET внутри CRM и не подменять `ProcessState` стадиями разговора. Не выкидывать холодных.

## Среда

- Локально: Docker Compose (Postgres 17 + приложение на Python 3.11), `.env.example` →
  `.env`. Тесты честнее гонять в контейнере — на хосте у Alena Python 3.13.
  `docker compose run --rm app pytest`.
- Интеграционные тесты требуют `TEST_DATABASE_URL` на мигрированную базу; внутри сети
  Docker хост называется `postgres:5432`, снаружи — `localhost:5433`.
- Внутренние sweep- и handoff-эндпоинты (`follow-up/run`, `integrations/deliver`,
  `commercial/expire`, `businesses/{id}/hot-leads`) требуют `INTERNAL_TASK_SECRET`
  на деплое, иначе отказывают всем запросам (by design). Секрет задаёт Alena.
- Облачная песочница Cowork **не имеет** pytest/fastapi/sqlalchemy и доступа к PyPI —
  если тесты там «проверены вручную», это значит самодельный раннер, а не pytest.
  В терминале и в Docker всё запускается по-настоящему; перепроверять стоит.
